ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
  application:
    modules:
      - com.ApplicationKt.module

jwt:
  domain: http://localhost:8080
  audience: budget-users
  realm: Budget Server
  secret: local-development-secret

database:
  url: jdbc:postgresql://localhost:5432/budget
  user: budget
  password: budget
  driver: org.postgresql.Driver
